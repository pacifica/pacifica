language: python
python:
 - 2.7

git:
  submodules: false
before_install:
 - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
 - git submodule update --init
 - git submodule sync
install: pip install -r docs/requirements-dev.txt
script: make -C docs/sphinx html
